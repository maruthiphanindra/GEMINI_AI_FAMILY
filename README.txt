First create  a virtual environment 
python3.10 -m venv venv

.\venv\Scripts\activate

then create a .env file and provide the API key 

in requirements file we have all the libraries that are nedded for our project 

in order to install those libraries run the command

pip install -r requirements.txt 

pip install -U langchain-community
